package com.visionarysoftwaresolutions.hfdpch2.tddstyle.demo.custom;

import java.io.File;
import java.util.ArrayList;

import com.visionarysoftwaresolutions.hfdpch2.tddstyle.observers.Display;

public class StationSetUp {
	
	public ArrayList<String> displaysToShow;
	
	public StationSetUp(){
	}

	public void promptForDisplays() {
		getInstalledDisplays();
		printInstalledList();
	}
	
	public void getInstalledDisplays(){
		String registryPath = "/home/nathan/Documents/training/HFDP/Ch2/Ch2.TDDStyle/DisplayInstallRegistry"; 
		File installFolder = new File(registryPath);
		File[] listOfFiles = installFolder.listFiles(); 
		for(int i = 0; i < listOfFiles.length; i++){
			if (listOfFiles[i].isFile()){
				displaysToShow.add(listOfFiles[i].getName());
				toClass();
				}
			}
		}
		
	private void printInstalledList(){
		
	}

}
